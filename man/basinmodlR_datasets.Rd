% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/data.R
\docType{data}
\name{basinmodlR_datasets}
\alias{basinmodlR_datasets}
\alias{brabant}
\alias{condroz}
\alias{ardennes}
\title{Example basins shipped with basinmodlR}
\format{
A list of six elements, described under Details.
}
\source{
Compiled from the phase-by-phase modelling scripts for the Anglo-Brabant
Massif and the Ardennes.
}
\usage{
brabant

condroz

ardennes
}
\description{
Three worked basin descriptions, each ready to be handed to
\code{\link{run_basin_model}}:
\describe{
  \item{\code{\link{brabant}}}{Brabant Massif, generalised 1-D model,
    525-0 Ma.}
  \item{\code{\link{condroz}}}{Huy, Condroz inlier, 520-0 Ma.}
  \item{\code{\link{ardennes}}}{Stavelot Massif, Cambro-Ordovician inliers of
    the Ardennes, 525-0 Ma.}
}
}
\details{
All three have the same structure, a list with the elements
\describe{
  \item{\code{name}}{character, the label used by \code{print} and by the
    figure.}
  \item{\code{units}}{data frame, one row per stratigraphic unit, oldest
    first, with the columns \code{name} (character), \code{start} and
    \code{end} (deposition interval in Ma, \code{start} being the older age),
    \code{thickness} (km at maximum burial, that is as deposited and before
    any later erosion of that unit), \code{phi0} (depositional porosity),
    \code{ck} (porosity decay coefficient, 1/km), \code{K} (matrix thermal
    conductivity, W/m/K), \code{A} (radiogenic heat production, uW/m3) and
    \code{rho} (grain density, kg/m3, carried through but not yet used).}
  \item{\code{erosions}}{data frame with \code{start}, \code{end} (Ma) and
    \code{amount} (km of section removed off the top of the column).}
  \item{\code{water_depth}}{two column matrix, age (Ma) and water depth (m).}
  \item{\code{heat_flow}}{two column matrix, age (Ma) and the heat flow into
    the base of the column (mW/m2).}
  \item{\code{source_units}}{integer, the rows of \code{units} that get a
    generation panel in \code{\link{plot_basin_model}}.}
}
Ages between the knots of \code{water_depth} and \code{heat_flow} are filled
in by linear interpolation, and the curves are held constant beyond their
first and last knot. Gaps in the timeline, such as 480-467 Ma at Brabant and
Huy, become hiatuses automatically.

The maximum-burial depth window of every unit, which layer-by-layer scripts
have to list by hand for each phase, follows from the units and the erosions
and is not part of the input. For Brabant that reproduces the published
numbers exactly: 9600 m of section at 420 Ma, 8600 m at 300 Ma and 6100 m
preserved today.

Two inconsistencies in the original scripts were repaired when these tables
were assembled. The Huy water depth curve had 15 ages but 14 values, so the
values were being recycled; the 310 Ma knot has been given the same value as
the 325 Ma knot, as in the Brabant curve. For the Ardennes, the oldest unit
had a depositional porosity of 0.56 in its own deposition phase and 0.595 in
every later phase, and the repeated value 0.595 is used here.
}
\examples{
str(brabant, max.level = 1)
brabant$units
condroz$erosions
ardennes$water_depth

}
\keyword{datasets}
